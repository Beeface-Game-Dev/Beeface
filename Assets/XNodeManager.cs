using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class XNodeManager : MonoBehaviour
{
  /*  public  enum ResourceTypes { Food, Mushroom, Crystal, Population}
    public ResourceTypes resourceType;

    public float harvestTime;
    public float resourceAmount;

    public int gatherers;
    // Start is called before the first frame update
    void Start()
    {
        StartCoroutine(ResourceTick());
    }

    // Update is called once per frame
    void Update()
    {
        if(resourceAmount <= 0)
        {
            Destroy(gameObject);
        }
    }

    public void ResourceGather()
    {
        if(gatherers != 0)
        {
            resourceAmount = resourceAmount - gatherers;
        }
    }

    IEnumerator ResourceTick()
    {
        while(true)
        {
            yield return new WaitForSeconds(1);
            ResourceGather();
        }
    }*/
}
