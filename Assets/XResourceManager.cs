using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class XResourceManager : MonoBehaviour
{
    /*public float Food;
    public float maxFood;
    public float Mushroom;
    public float maxMushroom;
    public float Crystal;
    public float maxCrystal;
    public float Population;
    public float maxPopulation;

    public Text FoodDisp;
    public Text MushroomDisp;
    public Text CrystalDisp;
    public Text PopulationDisp;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        FoodDisp.text = "" + Food + "/" + maxFood;
        MushroomDisp.text = "" + Mushroom + "/" + maxMushroom;
        CrystalDisp.text = "" + Crystal + "/" + maxCrystal;
        PopulationDisp.text = "" + Population + "/" + maxPopulation;

        if (Food >= maxFood)
        {
            Food = maxFood;
        }
    }*/
}
